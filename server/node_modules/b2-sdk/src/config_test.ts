import test = require('blue-tape')

import {readConfig} from './config'

test("config.readConfig", async function(t) {
  const config = await readConfig("./b2config.json")
  t.assert(!!config.endpoint)
})