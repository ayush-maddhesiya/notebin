import { AxiosRequestConfig } from "axios";
import { B2Meta } from "./meta";
export interface B2ClientConfig {
    endpoint: string;
    token: string;
}
export declare class B2Client {
    private config;
    constructor(config: B2ClientConfig);
    get endpoint(): string;
    url(path: string, query?: {}): string;
    configureQuery(query?: any): any;
    request(method: string, path: string, body?: any, config?: AxiosRequestConfig): Promise<any>;
    inspect(): Promise<B2Meta>;
}
