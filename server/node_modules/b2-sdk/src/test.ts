import {readConfig, B2Config} from './config'
import {B2Client} from './client'

export const TEST_CONFIG_FILE = './b2config.json'

export function getTestConfig(): Promise<B2Config> {
  return readConfig(TEST_CONFIG_FILE)
}

export async function getTestClient() {
  return new B2Client(await getTestConfig())
}